// Arrays

// Exercise: 
// Create an array of 10 (x,y) values.
// Then draw ellipses with each pair.

// This is our empty array
var circles = [];

function setup(){
  createCanvas(400,400);
  
  // Fill the array
}

function draw(){
  // Use the values in the array to set the position of ellipses
}